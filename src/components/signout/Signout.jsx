import React from "react";


const Signout = (props) => {

  return <div title="Sign out of Gmail" onClick={props.onSignout} className='btn btn-light bg-light ml-auto'>Sign Out</div>;
}

export default Signout;
